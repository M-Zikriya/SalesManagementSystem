<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salemanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaleManagementSystem">
        <activity android:name=".Product"></activity>
        <activity android:name=".CategoryEdit" />
        <activity android:name=".BrandEdit" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BrandView" />
        <activity android:name=".brand" />
        <activity android:name=".CategoryView" />
        <activity android:name=".CaregoryView" />
        <activity android:name=".Categoryview" />
        <activity android:name=".Category" />
        <activity android:name=".Main" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>